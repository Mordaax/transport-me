package sg.edu.np.mad.transportme;

import java.time.LocalDate;

public class WeekUtils { //as dateSelected is used across many activities, instead of constantly passing the value over, define it in WeekUtils
    public static LocalDate dateSelected;
}

